G-SPEED-STAGE0-REBASE — WIRE-CPU Stage-0 re-baseline, durable receipt (2026-07-02)
===================================================================================
CLOSES: the CONTRACT-SPEED §3 receipt gap ("CPU figures live only in commit messages; the
sp_toks harness + 0.6B sp-model are not on the box").

REBUILT PREREQS (all now on the box + tracked/regenerable):
- sp_toks.exe: build-cpu-perf (cmake -DSP_ENGINE_WITH_AVX512=ON -DSP_ENGINE_BUILD_TESTS=ON,
  clang-cl). THREE TOOLCHAIN FIXES were required (src/CMakeLists.txt, this commit):
  (1) clang-cl silently DROPPED the bare -mavx512* per-file flags (COMPILER_ID="Clang"
      matched the GNU branch) -> new MSVC-frontend branch passes them via /clang: escape;
  (2) -mavx512vl was MISSING from the flag list entirely (256-bit VPDPBUSD -> LLVM ISel
      crash; MSVC /arch:AVX512 had implied VL, hiding it historically);
  (3) __builtin_cpu_supports (avx512 dispatch) needs clang_rt.builtins-x86_64.lib under
      lld-link (__cpu_model etc.) -> auto-discovered + linked. cpu_overlay.c added to the
      per-file AVX512 flag list (its dot_i8_f32_avx512 was never covered under clang).
- qwen3-0.6b-q8.sp-model (720MB) + paired .sp-tokenizer: minted via sp_transcode --q8 from
  D:\Files\Models\Qwen\Qwen3-0.6B-GGUF\Qwen3-0.6B-f16.gguf (regenerable, not tracked).
  GOTCHA receipted: sp_toks derives the tokenizer path by EXTENSION SWAP — the pair must
  share the basename (qwen3-0.6b-q8.sp-tokenizer), else sp_model_load fails (-6).
- llama.cpp reference: llama-cli (build 2026-06-02) + Q8_0 quant minted via llama-quantize.
  GOTCHA: this llama-cli ignores -no-cnv/--no-conversation with a chat-template model
  (infinite "> " loop under redirect); use `-st` (--single-turn) for scripted timing runs.

MEASURED (i9-11900KB, 128 gen tokens, seed 1, same box same hour; sp_toks default prompt):
| leg                         | t1    | t5    | t8    |
|-----------------------------|-------|-------|-------|
| SP f16 GGUF (reference path)| 1.37 (default threads) — matches the 06-02 ladder bottom rung |
| SP OK_Q8 arena (swivel)     | 11.10 | 31.41 | 34.18 |
| llama.cpp Q8_0              |   —   | 52.5  | 53.2  |

DELTAS vs the June picture (receipts-first honesty — the old numbers were stale):
1. Gap NARROWED: 1.56x behind llama at t8 (34.18 vs 53.2), down from the recorded ~1.95x
   (27.3 vs ~53). Today's build is ~15% faster at t5 than the June note (31.4 vs 27.3).
2. The "SP saturates at 5 threads" claim NO LONGER HOLDS: t8/t5 = 1.09x (llama = 1.01x,
   already flat at 5). Thread-scaling has largely closed; the REMAINING gap is the
   bandwidth-efficiency term (the two-stream codes+scales layout / access pattern), as
   CONTRACT-SPEED §2 diagnosed.
3. The dot-ALU triple-negative stands untouched (no kernel work done here).

SPEED_NORTHSTAR KICKOFF NOTE (the contract's recommended lane B): the 35B-A3B MoE
.sp-model ALREADY EXISTS (models/qwen36-35b-a3b.sp-model, 16.3GB + tokenizer; branches
sprint/hx-3b + qwen36 refs in-repo; GGUF lane dead per GGUF-INVEST — safetensors-direct).
First falsifiable step: load + single-forward the 35B-A3B through the qwen36 path on this
box (16.3GB model vs 12GB VRAM -> the expert-residency/streaming design IS the campaign);
gate = coherent tokens at ANY speed, then the residency ladder.

ENV (re-baseline law): build-cpu-perf @ this commit (clang-cl 22, /clang: AVX512 flags, Ninja
Release); OMP_NUM_THREADS as tabled; SP_TOKS_N=128; model shas: f16 gguf (06-10 era disk),
OK_Q8 minted this session; llama-cli 2026-06-02 build, llama-quantize same.
Raw outs: _sp_toks_f16.out, _sp_toks_q8_t{1,5,8}.out, _llama_t{5,8}.out (untracked scratch;
numbers transcribed above).
