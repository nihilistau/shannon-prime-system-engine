G-N1-RECON — "exact-integer prefill GEMM" (N1) recon: RESOLVED-ALREADY-CLOSED (2026-07-02)
==========================================================================================
PRE-SCOPE (G-BX-OBEY-AB / RUNBOOK §10): "the residual float surface on the served path =
the prefill cuBLAS GEMMs; N1 = extend the exact-integer substrate to the kv-path prefill."

RECON (host-read, cuda_forward.cu):
1. `gemma4_kv_prefill` (line ~4338) = a LOOP of `g4_kv_step(s, do_head=(i==n-1))` — the
   IDENTICAL per-token step the decode uses. There is no separate batched prefill GEMM on
   the served kv path.
2. Inside g4_kv_step: the weight matmuls go through the `KMMD` macro —
   `use_int8 && gemv_w_packed(...)` = the OK_Q4B dp4a INTEGER GEMV (SP_CUDA_DECODE_INT8=1
   + arena, exactly the one-config); cuBLAS is only the non-int8 FALLBACK (single handle
   declaration in scope, unused on this config).
3. Per-request byteexact (default-ON) already routes islands (RMSNorm/RoPE/GELU) + decode
   attention through the exact-integer branches (G-BX-OBEY-AB).

VERDICT: **N1 has NO TARGET on the served one-config path** — prefill == decode kernels ==
integer GEMV + exact islands/attention. The G-BX-OBEY-AB mechanism GUESS ("prefill cuBLAS
GEMMs") is CORRECTED: no cuBLAS GEMM executes on this path. Every arithmetic stage on the
served path is either integer or a fixed-order kernel — fully consistent with the measured
facts (bit-identical across rebuilds/restarts/ballast/env today; the 07-01 receipt state
remains environmental-unreachable, G-CUBLAS-PIN-CANARY).

Residual float on the path (documented, deterministic-per-binary, NOT a cross-env risk we
can demonstrate): dp4a block-scale application (f16 scale x int accumulate -> f32, fixed
order), logit head + eot-bias f32 adds (fixed order). No build item follows. N1 CLOSED as
recon-resolved; the byte-exact envelope on the served path is: integer GEMV + exact islands
+ exact attention + fixed-order scale/head.

Scope note: the ONE-SHOT forward (gemma4_decode_cuda) and non-int8 configs DO use cuBLAS
float paths — out of scope for the one-config (SP_CUDA_DECODE_INT8=1 pinned there).
