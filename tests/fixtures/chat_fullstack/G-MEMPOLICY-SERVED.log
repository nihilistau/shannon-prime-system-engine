G-MEMPOLICY-SERVED — ADR-004 realized ON THE METAL: per-entry MEM-OKF policy drives the
SERVED spine's delivery, overriding the global env flag (SP_MEM_POLICY=1). (2026-07-03)
=========================================================================================
Carries G-MEMPOLICY-V3 (harness-level composition) to the served daemon. The recall path
(routes.rs L5 block) now reads the SELECTED entry's own mem_class/mem_delivery/mem_decline
(recall::MemPolicy, loaded per registry row) and dispatches the delivery/decline accordingly,
INSTEAD of the global SP_RECALL_L5_PROMPT / SP_RECALL_ATTR_GATE env flags. Gated SP_MEM_POLICY=1
(default-off = the env path, byte-identical).

BUILD (engine this-commit):
  * recall.rs: `MemPolicy { class, delivery, decline_when, decline_message }` + `Episode.policy`
    (loaded from the registry row's mem_class/mem_delivery/mem_decline_*; None ⇒ env path) +
    `class_default_delivery()` (mirror of okf_mem.py; two-stage REFUTED ⇒ same-template→systemecho).
  * routes.rs: the L5 delivery block resolves an effective `delivery_mode` = policy.delivery
    (attr-gate-strict recites when not declining) else SP_RECALL_L5_PROMPT; and `attr_gate` =
    env OR policy (attr-gate-strict / decline-when has attribute-absent|zero-inference). The
    5 delivery branches switch on `delivery_mode`; the zero-inference decline uses the entry's
    `mem_decline_message`. Logs `MEM-POLICY: entry '..' class=.. delivery=.. -> mode=..`.
  * scored tuple carries the per-episode policy; the selected episode's policy is looked up by name.

THE SERVED A/B (decisive) — same daemon, same GLOBAL flag SP_RECALL_L5_PROMPT=plain, same
V3 registry (30 rows tagged mem_class=counterfact). ONLY SP_MEM_POLICY differs:
  SP_MEM_POLICY=0 (env plain, no policy):   OBEY 11/30 (36.7%)  LEAK 17   (_v3_mempol_off.out)
  SP_MEM_POLICY=1 (per-entry policy):       OBEY 21/30 (70.0%)  LEAK  0   (_v3_mempol_on.out)
  => the entry's mem_class=counterfact -> systemecho delivery OVERRODE the global `plain` flag,
     ON THE METAL: obey 11->21, leak 17->0. The served log confirms per-entry dispatch:
     "MEM-POLICY: entry 'ep_live_m1783058137377' class=counterfact delivery=systemecho -> mode=systemecho".

VERDICT: GREEN. ADR-004 (memory as a governing layer) is now realized on the served spine, not
just the harness (G-MEMPOLICY-V3). The retrieved memory's OWN declared policy drives the
LatentDecision; the global env flag is the fallback. Default-off (SP_MEM_POLICY unset) = the
ADR-002 global path, byte-identical (an un-policied entry ⇒ env path). Safety monotone: a
`private-secret` entry forces attr-gate-strict + zero-inference decline regardless of the
global flag (the format + okf_mem.py verify forbid the downgrade).

WHAT'S NEXT (honest): (1) the served secret-class decline was proven at the harness (attr-gate
G-SNE-ATTRGATE-ZEROINF) but not yet re-run through a served private-secret episode (needs a
captured secret in the registry) — additive follow-on. (2) NIGHTSHIFT should assign mem_class
at capture (live episodes currently load policy=None ⇒ env path) so grown memories self-classify.
(3) point the served store at memory-okf/ (read policy from the OKF frontmatter) to unify the
served registry with the MEM-OKF v2 store.

REPRO: run_v3_mempolicy.bat {0|1} (global plain; arg=SP_MEM_POLICY) + python _v3_corpus/v3_run.py
gate <label>. Registry tagged via the mem_class add (registry_pretag.jsonl = pre-tag backup).
Serve log _v3_serve_mempolicy.log (MEM-POLICY dispatch lines). ENV: engine this-commit; RTX 2060.
