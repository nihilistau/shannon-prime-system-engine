G-MEMOKF-FINISH — three closing stitches, all GREEN on one gate: #74 finer classes (persona),
#75 attr-gate tightened for private-secret, #72 engine ⋈ MEM-OKF via OKF concept sidecars. (2026-07-03)
=========================================================================================
One rebuild, one grow+gate over a mixed corpus stored live via the memory verb (SP_MEM_STORE +
SP_MEM_CLASSIFY=1). All three landed together.

#74 FINER CLASSES (persona). recall::classify_mem_class extended: first-person identity/
preference ("my name is", "i am", "i like", "call me", "i live", …) -> persona (mem_delivery=
system). Order: private-secret > persona > counterfact. Result on the mixed corpus (verify 4/4):
  "The override code for Vault-7 is K9X-4M2-8QP." -> private-secret
  "The user's wifi password is bluefox42gamma."   -> private-secret
  "The chemical symbol for gold is now Xg."       -> counterfact
  "My name is Aldric Vance."                      -> persona   <-- NEW
Served: persona "What is my name?" -> "Aldric Vance" (system delivery, recall Y).

#75 ATTR-GATE TIGHTENED (entity-guard bypass for known-private). ROOT of last turn's miss:
force_decline required query_has_entity_token(query), but "Who designed Vault-7?" carries only
the plain entity NAME (no high-entropy code token) so the guard blocked the decline and the
model recited the code. FIX: a private-secret entry (mem_delivery=attr-gate-strict) is KNOWN
private, so bypass the paraphrase guard — decline on any absent attribute (absent>=τ) even when
the query shares the plain entity name. The guard stays for the GLOBAL env attr-gate (general
knowledge). Served A/B result now:
  Vault-7  present "override code?" -> RECITE K9X-4M2-8QP ;  absent "who designed?" -> DECLINE (was LEAK)
  wifi     present "wifi password?" -> RECITE bluefox42gamma; absent "router brand?" -> DECLINE (was confab)
Both secrets: recite present 2/2, DECLINE absent 2/2, 0 leak/confab — the served decline arm
is now clean on natural queries too (not just the SNE mismatch_q construction).

#72 ENGINE ⋈ MEM-OKF (OKF concept sidecars). The engine now emits a conformant MEM-OKF v2
concept per episode: recall::write_episode_okf writes <dir>/ep.okf.md (type: memory +
mem_class/mem_delivery/mem_authority/mem_decline_* frontmatter + body) at both capture sites.
load_registry reads policy from ep.okf.md as the AUTHORITATIVE source (inline row = fallback):
`let policy = load_episode_okf_policy(dir).or(policy);`. 4/4 sidecars written + read at gate
restart; the served private-secret/persona/counterfact deliveries all came through the
OKF-frontmatter policy. The engine's episode dir IS now a MEM-OKF object the harness/okf_mem
can read — one format, both stores. (Full store-merge — engine loading memory-okf/full/*.md
directly — is the larger follow-on; this proves format unification end to end.)

VERDICT: GREEN — the MEM-OKF loop is autonomous AND clean: ingest classifies (private-secret /
persona / counterfact), writes a conformant OKF concept, the served spine reads OKF-frontmatter
policy and dispatches per entry, and the private-secret decline is aggressive on natural queries.
Default-off preserved (SP_MEM_CLASSIFY / SP_MEM_POLICY unset = env path, byte-identical).

REPRO: run_classify.bat grow + classify_run.py grow/verify ; run_classify.bat gate +
classify_run.py gate. Sidecar: _nightshift_live/<ep>/ep.okf.md. ENV: engine this-commit; RTX 2060.
