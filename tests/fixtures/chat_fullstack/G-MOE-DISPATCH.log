G-MOE-DISPATCH — NORTHSTAR brick 6: batched expert phases — hypothesis FALSIFIED, small win kept (2026-07-02)
============================================================================================================
HYPOTHESIS (from brick 5's re-profile): ~960 OpenMP fork-joins/token over small expert
matmuls = dispatch-bound; batching should be a 1.5-2x.

BUILT: (a) moe_ffn arena fast path — ALL routed experts' gate+up rows in ONE parallel
region (flattened 2*NU*FF row space), silu-mul inside the region, all down rows in a second
region; k-order yo accumulation preserved => sequence-identical by construction. GGUF path
falls back to the per-expert loop. (b) matmul_arena: `omp parallel if(out >= 64)` — tiny
matmuls (gdn beta/alpha, 32 rows) stay serial.

GATE: SEQ 21/21 IDENTICAL again (760 6511 ... 264). VERDICT: GREEN (correctness), but:

RESULT: 1.605 -> 1.655 tok/s = **+3% only. The dispatch hypothesis is FALSIFIED** — libomp
keeps a persistent thread pool, so "fork-joins" were never real spawns; region overhead was
already negligible. Honest negative recorded; the code is KEPT (marginally faster, cleaner,
sequence-identical).

CORRECTED DIAGNOSIS (arithmetic, receipts-first): active bytes/token ≈ 0.7-1.0GB (gdn
projections ~380MB + head ~254MB + experts ~12MB + attn) in ~0.6s = **~1.4GB/s effective on
a ~30GB/s machine => NOT bandwidth-bound. The wall is ALU in the row-dot path: scalar
sp_frob_q4_unpack (per-nibble) + per-8 cvtepi8->ps conversion before the FMA.**

BRICK 7 (pre-scoped, the known cures — anti-rebuild pointers):
1. Fused nibble-unpack SIMD dot (unpack 32 nibbles in-register, no int8 materialization) —
   the llama.cpp kernel pattern; est 2-3x on Q4-heavy tensors.
2. OR int8-activation path: per-block act quant + int8xint8 maddubs/VNNI — the tree ALREADY
   HAS sp_avx512_q8blk_matvec (SP_Q8BLK, measured ~null on the 0.6B because THAT baseline
   was already vectorized; against THIS scalar-unpack baseline it should win big). The
   diffusion-judge campaign proved the act-quant numerics (~1e-3, parity rc=0).
3. THEN the GPU residency ladder (dg_gemm_packed_rows / hot-expert LRU vs 12GB) — likely
   the bigger jump than any further CPU rung.

THE LADDER (16-token greedy, 35B-A3B, i9-11900KB):
  stateless 0.018 | +state 0.165 (9.1x) | +OMP 1.136 (6.8x) | +SIMD 1.605 (1.41x)
  | +batch/if 1.655 (1.03x) = **92x cumulative, ~0.60s/token flat**
Profile now: moe 39% | gdn_mm 30% | head 15% | conv+recur 8% | attn 8%.
ENV: submodule this-commit on qwen36-gen-coherence; clang-cl /openmp /arch:AVX2.
Raw: _gen_spm_b6.out, _prof3.err.
