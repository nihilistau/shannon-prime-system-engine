G-MOE-FORWARD-35B — SPEED_NORTHSTAR brick 1: the 35B-A3B forward, verified on today's box (2026-07-02)
======================================================================================================
GOAL (CONTRACT-SPEED lane B kickoff): load + forward the Qwen3.6-35B-A3B (qwen35moe: 40 blocks,
10 full-attn + 30 Gated-DeltaNet linear-attention, IMRoPE [11,11,10,0], 256 experts / 8 used,
per-expert n_ff=512) and establish the honest starting state for the residency campaign.

RECON (anti-rebuild — most of the brick already existed):
- SPEC-qwen35moe-GDN.md = the ground-truth spec (GDN, NOT Mamba2 — the GGUF-INVEST mislabel is
  superseded by the spec; the archived doc's "cannot be incremental" verdict was answered by the
  Phase-3 build: core/forward/qwen36.c + qwen36_gate.c + the Stage-3 transcoder 3c5f370).
- BOTH artifacts on the box: the 19.7GB Q4_K_M GGUF (lmstudio-community) AND the 16.3GB
  OK-container models/qwen36-35b-a3b.sp-model (+ tokenizer).

LEG 1 — GGUF-direct forward (M_QWEN36 gate, rebuilt + rerun today): **PASS 3/3**
  tok0 SP=5444 oracle=5444 OK | tok1 SP=8 oracle=8 OK | tok2 SP=198 oracle=198 OK
  "qwen35moe forward top-1 bit-exact to oracle" — the GDN+MoE+IMRoPE CPU forward is alive and
  correct on the current tree (build-syscore-tests standalone configure; ~1 min for load+3 fwd,
  i.e. CPU A3B forwards are workably fast).

LEG 2 — OK-container round-trip (qwen36_spmodel_top1, FIRST 35B measurement): **2/3**
  arch_id=8 interval=4 n_expert=256/8 n_ff_exp=512 gdn_state=128/16/32; V=248320; loads clean.
  SPM0 5444 OK | SPM1 11 vs 8 MISMATCH | SPM2 198 OK -> C1_ROUNDTRIP_FAIL by the 3-token
  bit-parity criterion. HONEST READING: no prior GREEN exists for the 35B container round-trip
  (the C1 top-1-bit-exact receipts cover the 0.5B coder; the README's 5-family claim covers the
  GGUF-direct forward). A single near-tie argmax flip between TWO DIFFERENT 4-bit quants
  (OK_Q4 container vs the Q4_K_M-derived oracle) is the expected regime per the established
  N1b reframe ("byte-exact logit parity across quants is impossible by construction — do not
  chase it"). The right container gate for the 35B is COHERENCE/PPL, not 3-token bit-parity.

BUILD GOTCHAS (receipted for the next builder):
- test_qwen36_gate: not a target of the engine build — configure the math-core STANDALONE
  (cmake -S lib/shannon-prime-system -B build-syscore-tests).
- qwen36_spmodel_top1.c: no CMake target anywhere; compile directly with clang-cl /MD +
  core/session/sp_model_bridge.c as a second TU + the ENGINE build's libs (the standalone
  syscore libs do NOT contain sp_model_load/the bridges) + clang_rt.builtins + libomp.
- (Stage-0's clang-cl AVX512 fixes are prerequisites for the engine-lib build.)

NEXT (the residency ladder, pre-scoped):
1. G-MOE-COHERENCE: 32-64-token greedy generation from a real prompt on the sp-model container
   (needs the qwen36 decode path wired to a small gen harness; decode.c persistent-KV decode is
   shipped per the math-core README) + a PPL sanity vs the GGUF-direct forward. Closes the
   container question the right way.
2. G-MOE-TOKS-CPU: tok/s baseline for the A3B on this i9 (the CPU may be surprisingly usable —
   3B active params ~= the 0.6B-dense regime x5).
3. RESIDENCY DESIGN: 16.3GB model vs 12GB VRAM -> hot-expert LRU + streaming; REUSE the proven
   dp4a packed-expert machinery from the diffusion-judge campaign (dg_gemm_packed_rows /
   gemv_w_packed, 43.9x there) — the kernels and the PCIe-collapse lesson transfer.

ENV: engine @ a8e1df8 tree; build-syscore-tests (clang-cl 22 standalone) + build-cpu-perf libs;
GGUF = D:\Files\Models\lmstudio-community\Qwen3.6-35B-A3B-GGUF\Qwen3.6-35B-A3B-Q4_K_M.gguf;
sp-model = models/qwen36-35b-a3b.sp-model (16.3GB). Raw: _qwen36_gate.{out,err}, _qwen36_sp.out.
